 "من فضلك، اتبع نظام العمل الموضح في WORKFLOW_PROTOCOL.txt"

"اتبع نظام العمل في WORKFLOW_PROTOCOL.txt"

# 🎯 WORKFLOW PROTOCOL - بروتوكول العمل

> **آخر تحديث:** 2025-11-22  
> **الهدف:** توضيح توزيع المسؤوليات بين المطور والمستخدم لضمان سير العمل بسلاسة

---

## 📌 القاعدة الأساسية

```
┌─────────────────────────────────────────┐
│  المطور = البرمجة والإعداد الكامل      │
│  المستخدم = تنفيذ أوامر الترمينال فقط │
└─────────────────────────────────────────┘
```

---

## 👨‍💻 مسؤوليات المطور (Antigravity)

### ✅ التعديلات البرمجية (100%)

**المطور مسؤول عن:**
1. **فتح وتعديل الملفات:**
   - فتح أي ملف Python/JSON/Config
   - إضافة/حذف/تعديل أي أسطر برمجية
   - إنشاء ملفات جديدة
   - حذف ملفات قديمة

2. **التأكد من صحة الكود:**
   - مراجعة Syntax
   - التأكد من Imports
   - اختبار المنطق البرمجي

3. **التسليم النهائي:**
   - **الملفات المعدلة جاهزة 100%**
   - لا حاجة لأي تعديل يدوي من المستخدم

### ✅ إعداد قائمة الأوامر

**المطور يجهز:**
```plaintext
═══════════════════════════════════════
📋 الأوامر المطلوبة (نسخ ولصق):
═══════════════════════════════════════

1️⃣ الأمر الأول:
   Copy-Item "file1.py" "file2.py"

2️⃣ الأمر الثاني:
   python main.py

3️⃣ الأمر الثالث:
   ...
```

---

## 👤 مسؤوليات المستخدم

### ✅ تنفيذ أوامر الترمينال فقط

**دورك يقتصر على:**
1. **فتح PowerShell**
2. **نسخ الأوامر من القائمة**
3. **لصقها في الترمينال**
4. **الضغط على Enter**
5. **إرسال النتيجة للمطور**

### ❌ ما لا تفعله

**لن تحتاج أبداً إلى:**
- ❌ فتح ملفات `.py` لتعديلها
- ❌ إضافة أسطر برمجية يدوياً
- ❌ حذف أو نقل أكواد
- ❌ التعامل مع Syntax Errors
- ❌ فهم تفاصيل الكود البرمجي

**كل هذا مسؤولية المطور بالكامل.**

---

## 🔄 سير العمل المعتاد

### المرحلة 1: الطلب

**المستخدم يرسل:**
```
أحتاج إضافة ميزة X
أو
أحتاج إصلاح مشكلة Y
```

---

### المرحلة 2: التحليل والبرمجة (المطور)

**المطور يقوم بـ:**
1. ✅ تحليل المشكلة
2. ✅ كتابة/تعديل كل الملفات المطلوبة
3. ✅ التأكد من صحة الكود
4. ✅ إنشاء الملفات الجديدة (إن وجدت)
5. ✅ **تسليم الملفات جاهزة 100%**

---

### المرحلة 3: التسليم (المطور)

**المطور يرسل:**

```
═══════════════════════════════════════════════
✅ التعديلات المطلوبة - جاهزة للتنفيذ
═══════════════════════════════════════════════

📁 الملفات المعدلة:
✅ modules/feature.py - تم التعديل
✅ main.py - تم التعديل
✅ config.json - تم التعديل

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 الأوامر المطلوبة (نسخ ولصق):

1️⃣ إعادة تشغيل البوت:
   cd "c:\Users\pc\Downloads\webapp جوجل"
   python main.py

2️⃣ اختبار الميزة:
   [خطوات الاختبار...]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ النتيجة المتوقعة:
[وصف النتيجة...]

⚠️ Rollback (إذا حدثت مشكلة):
git checkout HEAD -- modules/feature.py
```

---

### المرحلة 4: التنفيذ (المستخدم)

**المستخدم يقوم بـ:**
1. ✅ نسخ الأمر الأول
2. ✅ لصقه في PowerShell
3. ✅ الضغط على Enter
4. ✅ نسخ الأمر الثاني... وهكذا
5. ✅ إرسال النتيجة للمطور

---

### المرحلة 5: التحقق (المطور)

**المطور يراجع:**
- النتيجة/اللوج المرسل
- التأكد من نجاح التنفيذ
- حل أي مشاكل إن وجدت

---

## 📋 مثال عملي كامل

### **الطلب:**
> "أحتاج إضافة ميزة تسجيل الأخطاء في ملف منفصل"

---

### **التسليم من المطور:**

```
═══════════════════════════════════════════════
✅ ميزة تسجيل الأخطاء - جاهزة للتنفيذ
═══════════════════════════════════════════════

📁 الملفات المعدلة:
✅ utils/error_logger.py - تم إنشاؤه (ملف جديد)
✅ main.py - تم التعديل (السطر 15: إضافة import)
✅ core.py - تم التعديل (السطر 200: استدعاء logger)

ملاحظة: كل الملفات معدلة وجاهزة بالكامل.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 الأوامر المطلوبة (نسخ ولصق):

1️⃣ إنشاء مجلد logs:
   New-Item -ItemType Directory -Path "logs" -Force

2️⃣ إعادة تشغيل البوت:
   python main.py

3️⃣ اختبار تسجيل الأخطاء:
   # قم بتشغيل أي عملية تسبب خطأ
   # تحقق من وجود ملف logs/error.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ النتيجة المتوقعة:
- البوت يعمل بشكل طبيعي
- عند حدوث خطأ، يتم تسجيله في logs/error.log
- console يطبع: "✅ Error logger initialized"

⚠️ Rollback (إذا حدثت مشكلة):
git checkout HEAD -- main.py core.py
Remove-Item utils/error_logger.py
```

---

### **التنفيذ من المستخدم:**

```powershell
# نسخ ولصق الأمر 1
New-Item -ItemType Directory -Path "logs" -Force

# نسخ ولصق الأمر 2
python main.py

# اتباع خطوات الاختبار...
```

---

### **إرسال النتيجة:**
```
النتيجة:
✅ البوت شغّال
✅ ملف error.log تم إنشاؤه
✅ console طبع: "✅ Error logger initialized"

كل شيء يعمل بشكل ممتاز!
```

---

## ⚠️ القواعد الذهبية

### ✅ **Do's - افعل:**

**للمطور:**
- ✅ عدّل كل الملفات بنفسك
- ✅ سلّم الملفات جاهزة 100%
- ✅ اكتب أوامر واضحة للترمينال
- ✅ اختبر الكود قبل التسليم

**للمستخدم:**
- ✅ نفّذ أوامر الترمينال فقط
- ✅ انسخ والصق بالضبط كما هي
- ✅ أرسل النتيجة/اللوج للمطور
- ✅ اطلب توضيح إذا كان هناك غموض

---

### ❌ **Don'ts - لا تفعل:**

**للمطور:**
- ❌ لا تطلب من المستخدم تعديل ملفات برمجية
- ❌ لا تفترض أن المستخدم يفهم Python
- ❌ لا تترك ملفات "نصف جاهزة"
- ❌ لا تنفذ أوامر تلقائياً (استخدام run_command ممنوع)

**للمستخدم:**
- ❌ لا تحاول تعديل الكود يدوياً
- ❌ لا تفتح ملفات `.py` لتغيير أي شيء
- ❌ لا تحذف/تنقل ملفات بدون أمر صريح
- ❌ لا تخترع أوامر من عندك

---

## 🎯 الخلاصة

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  المطور: يكتب كل الكود + يجهز الأوامر               │
│           ↓                                             │
│  المستخدم: ينفذ الأوامر في الترمينال فقط             │
│           ↓                                             │
│  النتيجة: مشروع مستقر + لا أخطاء + لا سوء فهم       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📞 **عند طلب تعديل جديد:**

**ببساطة قل:**
> "من فضلك، اتبع نظام العمل الموضح في `WORKFLOW_PROTOCOL.txt`"

**وسيكون الرد:**
> "✅ حاضر! سأجهز كل الملفات المعدلة + قائمة الأوامر للتنفيذ اليدوي"

---

## 📝 تاريخ التحديثات

| التاريخ | التغيير |
|---------|---------|
| 2025-11-22 | إنشاء البروتوكول الأولي |

---

**🤝 بهذا البروتوكول، نضمن:**
- ✅ سير عمل سلس
- ✅ لا تعقيدات برمجية على المستخدم
- ✅ لا تنفيذ تلقائي يسبب مشاكل
- ✅ وضوح تام في المسؤوليات

**🎉 تعاون احترافي وفعّال!**
